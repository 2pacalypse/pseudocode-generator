#include <stdio.h>
#include <stdlib.h>

int main(){
	int first, second=3, third=f, Factorial=1, b;
	char str = 'd';
	int i;
	int MAX;
	scanf("%d", &MAX);
	for(i=1; i <= first; i++){
		if(first < 0){
			Factorial = -1;
			if(second < 0){
				Factorial = -2;
				for(j=1; j <= MAX; j++){
					printf("%d\n", FACTORIAL);
				}
			}
		}else if (MAX > 10){
			Factorial = -1;
			i = MAX + 1;
		}else if (MAX <= 10){
			Factorial = -1;
			i = MAX + 1;
		}else {
			Factorial = -1;
			i = MAX + 1;
		}
	}

	printf("%d\n", FACTORIAL);
	a = multFunc(a, b);
}



int multFunc(int x, int y)
{
x = x*y;
return x;
}
