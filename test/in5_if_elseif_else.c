#include <stdio.h>
#include <stdlib.h>

int main(){
	int Factorial = 0;
	if(MAX < 0){
		Factorial = -1 * -2 * -3 * 4 - 5 + 6 / 7;
	}else if (MAX <= 5){
		Factorial = -2;
		i = MAX + 2;
	}else if (MAX >= Factorial){
		Factorial = -3;
		i = MAX + 3;
	}else if (Factorial == i){
		Factorial = -4;
		i = MAX + 4;
	}else {
		Factorial = 0;
		i = MAX + 5;
	}
	return 0;
}

