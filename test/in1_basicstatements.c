#include <stdio.h>
#include <stdlib.h>

int main(){
	int a, b=3, d=f, Factorial=1, b;
	char str = 'd';
	int i;
	int MAX;
	scanf("%d", &MAX);
	printf("%d", MAX);
	i = 5 + 6 + 7 + 8 + MAX;
	return i;
}

